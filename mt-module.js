export default (name) => {
   // console.log("hello"+name);
    console.log('hellooğ ${name}');
};
const topla = (a,b) => a+b;
const cikar = (a,b) => a-b;
const text = "TEXT";
const user = {
    name : "hacer",
    surname : "bakir"
}

const users = [{
    name: 'beyza',
    surname: 'adasfd'
},{
    name: 'ali',
    surname: 'asfdgfsd'
}]

export { topla, cikar, text, user, users};
//export default hello;